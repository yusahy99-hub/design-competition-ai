services:
  - type: web
    name: design-competition-ai-backend
    runtime: docker
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: DATABASE_URL
        value: sqlite+aiosqlite:///./data/competitions.db
